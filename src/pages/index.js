import React from 'react'
import UploadImage from '../components/UploadImage'

const Upload = (props) => (
 	<UploadImage
 		allBrandNames={props.allBrandNames}
 		allBrandAccounts={props.allBrandAccounts}
 	/>
)

export default Upload
